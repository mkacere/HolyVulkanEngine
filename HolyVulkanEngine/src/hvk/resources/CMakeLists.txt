# {ENGINE_ROOT}/hvk/resources
file(GLOB_RECURSE HVK_RES_SOURCES CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_LIST_DIR}/*.cpp
)

target_sources(HVKEngine PRIVATE ${HVK_RES_SOURCES})

source_group(TREE ${CMAKE_CURRENT_LIST_DIR} FILES ${HVK_RES_SOURCES})
