# {ENGINE_ROOT}/hvk/core
file(GLOB_RECURSE HVK_CORE_SOURCES CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_LIST_DIR}/*.cpp
)

target_sources(HVKEngine PRIVATE ${HVK_CORE_SOURCES})

source_group(TREE ${CMAKE_CURRENT_LIST_DIR} FILES ${HVK_CORE_SOURCES})
