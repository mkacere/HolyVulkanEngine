# {ENGINE_ROOT}/hvk/systems
file(GLOB_RECURSE HVK_SYS_SOURCES CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_LIST_DIR}/*.cpp
)

target_sources(HVKEngine PRIVATE ${HVK_SYS_SOURCES})

source_group(TREE ${CMAKE_CURRENT_LIST_DIR} FILES ${HVK_SYS_SOURCES})
