# {ENGINE_ROOT}/hvk/scene
file(GLOB_RECURSE HVK_SCENE_SOURCES CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_LIST_DIR}/*.cpp
)

target_sources(HVKEngine PRIVATE ${HVK_SCENE_SOURCES})

source_group(TREE ${CMAKE_CURRENT_LIST_DIR} FILES ${HVK_SCENE_SOURCES})
