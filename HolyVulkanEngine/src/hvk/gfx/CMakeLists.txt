# {ENGINE_ROOT}/hvk/gfx
file(GLOB_RECURSE HVK_GFX_SOURCES CONFIGURE_DEPENDS
    ${CMAKE_CURRENT_LIST_DIR}/*.cpp
)

target_sources(HVKEngine PRIVATE ${HVK_GFX_SOURCES})

source_group(TREE ${CMAKE_CURRENT_LIST_DIR} FILES ${HVK_GFX_SOURCES})
